// vue.config.js
module.exports = {
    // webpack config
    // 该对象将会被 webpack-merge 合并入最终的 webpack 配置。
    configureWebpack: {
        plugins: [
            // new MyAwesomeWebpackPlugin()
        ]
    }
}